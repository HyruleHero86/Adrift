using UnityEngine;

public class WireSceneFromMain : MonoBehaviour
{
    void WIreSceneLoad()
    {
        // Make sure the cursor is visible and unlocked
        Cursor.lockState = CursorLockMode.None;
        Cursor.visible = true;
    }
}
